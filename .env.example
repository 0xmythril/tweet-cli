# X/Twitter API v2 credentials
# Get these from https://developer.x.com/en/portal/dashboard
#
# 1. Create an app (or use existing)
# 2. Under "User authentication settings", set permissions to "Read and write"
# 3. Generate (or regenerate) keys and tokens
#
# Copy this file to ~/.config/tweet-cli/.env and fill in your values.

X_API_KEY=your_consumer_key
X_API_SECRET=your_secret_key
X_ACCESS_TOKEN=your_access_token
X_ACCESS_TOKEN_SECRET=your_access_token_secret
